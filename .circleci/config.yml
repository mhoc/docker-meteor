version: 2
workflows:
  version: 2
  build:
    jobs:
      - build-15
      - build-16
jobs:
  build-15:
    docker:
      - image: docker:17.05.0-ce-git
    steps:
      - checkout
      - setup_remote_docker
      - run:
          name: build docker image
          command: |
            docker build -t app -f Dockerfile-1.5 .
  build-16:
    docker:
      - image: docker:17.05.0-ce-git
    steps:
      - checkout
      - setup_remote_docker
      - run:
          name: build docker image
          command: |
            docker build -t app -f Dockerfile-1.6 .
