version: 2
workflows:
  version: 2
  build:
    jobs:
      - build-15
      - build-16
jobs:
  build-15:
    docker:
      - image: docker:17.05.0-ce-git
    steps:
      - checkout
      - setup_remote_docker
      - run:
          name: build docker image
          command: |
            docker build -t app -f 1.5.dockerfile .
  build-16:
    docker:
      - image: docker:17.05.0-ce-git
    steps:
      - checkout
      - setup_remote_docker
      - run:
          name: build docker image
          command: |
            docker build -t app -f 1.6.dockerfile .
  build-17:
    docker:
      - image: docker:17.05.0-ce-git
    steps:
      - checkout
      - setup_remote_docker
      - run:
          name: build docker image
          command: |
            docker build -t app -f 1.7.dockerfile .
